<Context allowLinking="true">
  <Manager pathname="" /> <!-- disable session persistence since it doesn't work for OAuth anyway -->

  <Resource name="jdbc/addiscore" auth="Container"
            type="javax.sql.DataSource" driverClassName="org.postgresql.Driver"
            url="jdbc:postgresql://localhost/addiscore"
            username="addiscore" password="develop"
            maxActive="20" maxIdle="10" maxWait="-1"/>
</Context>
